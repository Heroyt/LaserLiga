get:
    summary: Get player info
    parameters:
        -   $ref: '../../parameters/path/playerCode.yaml'
    responses:
        '200':
            description: User data
            content:
                application/json:
                    schema:
                        $ref: '../../schemas/user/UserData.yaml'
        400:
            $ref: '../../responses/400.yaml'
        401:
            $ref: '../../responses/401.yaml'
        500:
            $ref: '../../responses/500.yaml'