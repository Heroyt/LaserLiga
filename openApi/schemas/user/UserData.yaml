description: Information of a registered user
type: object
properties:
    id:
        type: integer
    code:
        $ref: './UserCode.yaml'
    nickname:
        type: string
    arena:
        type: integer
        description: Home arena's ID
    email:
        type: string
        format: email
    stats:
        $ref: './UserStats.yaml'
    connections:
        type: array
        items:
            $ref: './UserConnection.yaml'